import React from "react";
import cls from "./GlobalButton.module.css";

const GlobalButton = () => {
  return (
      <button className={cls.Button}>Settins</button>
  );
};

export default GlobalButton;
