import React from "react";
import cls from "./SettingsButton.module.css";

const SettingsButton = () => {
  return (
      <button className={cls.SettingsButton}></button>
  );
};

export default SettingsButton;
